import React from "react";

const Title = ({ text }) => {
    return(
        <div>
            <center>
                <label>{text}</label>
            </center>
        </div>
    )
};

export default Title;